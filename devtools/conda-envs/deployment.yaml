name: latest-deployment
channels:
  - conda-forge
dependencies:
  - openff-bespokefit
  - ambertools
  - xtb-python
  - pytest
  - pytest-cov
  - pytest-asyncio
  - requests-mock
variables:
  # This should be unnecessary in the release after 0.4.3; see #407
  PYTEST_PLUGINS: celery.contrib.pytest
