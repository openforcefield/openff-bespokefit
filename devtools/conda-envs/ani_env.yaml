name: basic_bespokefit
channels:
  - conda-forge
  - omnia
  - omnia/label/rc
  - pytorch
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - openforcefield >= 0.7.0
  - openforcefields
  - qcengine >=0.16.0
  - forcebalance
  - qcsubmit
  - pyyaml
  - openeye-toolkits ==2019.10.2
  - chemper
  - torchani


    # Pip-only installs
  - pip:
      - basis_set_exchange
      - git+git://github.com/openforcefield/fragmenter.git@0.0.7#egg=fragmenter