name: test

channels:
  - openeye
  - conda-forge
  - defaults

dependencies:

    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    ### Core dependencies.

  - pydantic
  - pyyaml
  - tqdm
  - click
  - rdkit
  - openff-toolkit-base
  - openff-forcefields
  - openff-qcsubmit

    # Optional
  - forcebalance
  - openff-fragmenter

  - openeye-toolkits

    ### Bespoke dependencies
  - qcengine >=0.18.0
  - qcfractal >=0.15.4
  - chemper
  - basis_set_exchange
