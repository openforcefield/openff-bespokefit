name: test

channels:
  - openeye
  - conda-forge
  - omnia
  - defaults

dependencies:

    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    ### Core dependencies.

  - pydantic
  - pyyaml
  - tqdm
  - openff-toolkit-base
  - rdkit
  - openff-forcefields
  - click

    # openff-qcsubmit needed until release
  - qcportal
  - torsiondrive
  - cachetools
    # / openff-qcsubmit

    # Optional
  - forcebalance
  - openmmforcefields # Needed until QCSubmit makes it optional

    ### Bespoke dependencies
  - qcengine >=0.18.0
  - qcfractal >=0.15.4
  - chemper
  - openeye-toolkits
  - basis_set_exchange

  - pip:
      - git+git://github.com/openforcefield/openff-qcsubmit.git@master
      - git+git://github.com/openforcefield/openff-fragmenter.git@master