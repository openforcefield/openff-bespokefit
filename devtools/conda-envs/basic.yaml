name: test

channels:
  - openeye
  - conda-forge
  - omnia
  - omnia/label/rc
  - defaults

dependencies:

    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    ### Core dependencies.

  - pydantic ==1.7.2
  - pyyaml
  - tqdm
  # - openff-toolkit-base
  # - rdkit
  - openforcefield == 0.8.0
  - openforcefields

    # Optional
  - forcebalance

    ### Bespoke dependencies
  - qcengine >=0.17.0
  - openff-qcsubmit ==0.2.1
  - openeye-toolkits ==2019.10.2
  - chemper
  - fragmenter ==0.0.7
  - basis_set_exchange
