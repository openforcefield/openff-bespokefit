name: basic_bespokefit
channels:
  - conda-forge
  - omnia
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - openforcefield >= 0.7.0
  - openforcefields
  - smirnoff99Frost
  - qcengine >=0.15.0
  - pyyaml
  - qcportal
  - openeye-toolkits
  - qcfractal
  - torsiondrive
  - fragmenter
  - xtb-python

    # Pip-only installs
  - pip:
      - basis_set_exchange
  #  - codecov

