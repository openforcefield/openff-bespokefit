name: basic_bespokefit
channels:
  - conda-forge
  - omnia
  - omnia/label/rc
  - openeye
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - openforcefield >= 0.7.0
  - openforcefields
  - qcengine >=0.16.0
  - forcebalance
  - qcsubmit
  - pyyaml
  - chemper
  - openeye-toolkits ==2019.10.2

    # Pip-only installs
  - pip:
      - basis_set_exchange
  #  - codecov

